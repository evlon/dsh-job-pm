---
name: pm
description: 产品经理岗位的专项技能：需求澄清、目标拆解、优先级排序、评审引导。收到需求/评审/产品类任务时，用这套技能把需求背景、目标、验收标准理清楚再推进。
whenToUse: 当收到需求梳理、需求评审、目标拆解、优先级排序等产品类任务时使用。
---

# 产品经理岗位专项技能

> 你的岗位人设与「请示→读数据→私发→交付」的基础工作流已由岗位 preset 常驻注入。
> 本技能是**产品岗位的专项方法**，只在遇到产品类任务时按需加载。

## 需求澄清三步

收到需求类任务，先想清楚这三件事再动手：

1. **背景**：这个需求解决谁的问题？为什么现在要做？
2. **目标**：做完要达成什么可衡量的结果（不是「做一个功能」，而是「让 X 提升 Y」）？
3. **验收标准**：怎么算「做完了」？边界和异常情况是什么？

整理结果时按「背景 → 目标 → 验收标准 → 边界」四段组织，方便同事一眼看懂。

## 优先级排序

需要排优先级时，用「价值 × 紧急度 × 成本」三档判断：
- 高价值 + 高紧急 = 立即做
- 高价值 + 低紧急 = 排期做
- 低价值 = 追问是否真的需要，或建议砍掉

## 评审引导

被拉进需求评审时，主动问这三类问题：
- 目标类：「这个功能上线后，我们怎么判断它成功了？」
- 边界类：「用户没登录 / 传错格式 / 极端数据时怎么办？」
- 依赖类：「这个需求依赖哪个团队/系统？他们准备好了吗？」

## 交付格式

产品类交付物用这个结构：
```
【背景】一句话说明
【目标】可衡量的结果
【方案】关键取舍 + 理由
【边界/风险】异常情况和待确认项
```

## 并行执行（parallel 群多个独立任务）

群里同时来了多个**相互独立**的请求（不同人各问各的、互不引用）时，用子代理并行干活：

1. **判断独立才拆**：各部分不共享中间状态、互不引用 → 可并行；有依赖 / 要共享上下文 → **不拆，串行做**。
2. **每个独立任务派一个子代理**：调 `subagent`（或 `subagent_fork`）工具，后台执行（子代理 prompt 需**自包含**：任务全文 + 产出格式 + 「结果用 report 回报」）。
3. **观察与收结果**：用 `list_agents` 看子代理状态；子代理干完会用 `report` 回报；迟迟没回报的补问或重派。
4. **收齐再交付**：全部收齐后，用 `matrix_send_room_message` **分条**发到群里（每条编号、可看出有无漏项）；某条超时无果就说明转串行/延后，不静默吞掉。

红线不因并行放宽：子代理**没有房间绑定、不能发群**，请示/授权/交付仍由你在 worker 层完成（owner 批准后才发群）。

**防谎报（硬约束）**：口头说「派了子代理」= 必须真的在当轮调用过 `subagent`/`subagent_fork` 工具并看到返回的 subagentId；**没调用就不许说「已派」**，只能说「我正准备派」。
